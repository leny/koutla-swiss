/* leny/koutla-swiss
 *
 * ~/mixins/size.js - Size mixin
 *
 * coded by leny@flatLand!
 * started at 13/01/2018
 */

export const size = (mWidth = 0, mHeight = mWidth) => ({
    width: `${mWidth}`,
    height: `${mHeight}`,
});
